export interface User{
    uid: string;
    email: string;
    password: string
    name: string
}


export interface User2 {
    nombre: string;
    categoria: string;
    fecha: string; // Suponiendo que la fecha es una cadena en formato de fecha válido
    valor: number; // Suponiendo que el valor es un número
  }